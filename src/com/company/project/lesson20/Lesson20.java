package com.company.project.lesson20;

import com.company.project.lesson08.Vehicle;
import com.company.project.lesson11.Article;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class Lesson20 {
    private Arrays vehicles;
    //преобразование stream в список
    List<Vehicle> vehicleList = vehicles.stream():

    //vehicles.stream().allMatch(vehicle -> vehicle.getLevelOfWare() > 100);


    /*vehicles.stream()
    .peek(vehicle -> vehicle.changeColor(Repaintable.Color.GOLD))
    .forEach(System.out.println);
     */

    //разобрать тип Optional - вместо if

    //vehicle.stream().collect();
    /*public static List<Article> task03(ArrayList<Article> articles, Article.Category category, int min, int max);
    return articles.stream()
            .filter(article -> article.getCategory() == category)
            .filter(article -> article.get )*/
}
